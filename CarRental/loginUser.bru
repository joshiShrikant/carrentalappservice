meta {
  name: loginUser
  type: http
  seq: 3
}

post {
  url: http://localhost:8090/api/v2/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "user1",
    "password": "password123"
  }
}

tests {
  // Assume the response body is the token itself
  let response = res.body;
  
  // Save it as a collection variable
  bru.setVar("authToken", response.token);
  
}

settings {
  encodeUrl: true
}
